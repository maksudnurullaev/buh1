<div class="toolbar">
  % if( $action ne 'calculations' ){
   <%= link_to "/$controller/calculations/$payload" => begin %><%= ml 'Calculations' %><% end %>
  % } else {
   <%= ml 'Calculations' %>  
  % }
  |
  % if( $action ne 'calculations_add_new' ){
   <%= link_to "/$controller/calculations_add_new/$payload" => begin %><%= ml 'New' %><% end %>
  % } else {
   <%= ml 'New' %>  
  % } 
</div>
