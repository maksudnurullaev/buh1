% my $files_count = files_count($_id) ;
% my $calcs_count = calcs_count($_id) ;
<sup><%= tag 'a', href => "/$controller/files/$_id", $files_count %> | <%= tag 'a', href => "/$controller/calculations/$_id", $calcs_count %></sup>
