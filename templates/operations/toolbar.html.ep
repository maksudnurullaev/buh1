<div class="toolbar">
 % if( $action eq 'edit' || $action eq 'add' ) {
  %= tag 'a', href => "/operations/account/$payload", ml('Back') ;
 % } else {    
  %= tag 'a', href => "/operations/list", ml('Back') ;
 % } 
 % if ( who_is_global('editor') && ($action eq 'account') ) {
  | 
 % }
