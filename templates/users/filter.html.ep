<div>
 % my $path = '/users/list';
 %= form_for '/users/filter' => ( method => 'POST') => begin
  %= text_field filter => (stash 'filter')
  %= hidden_field path => $path
  %= submit_button ml 'Filter'
  %= link_to ml("Reset") => "/users/nofilter?path=$path"
 % end 
</div>

