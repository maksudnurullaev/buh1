% my $companies = stash 'companies';
% if ( $companies ) {
  <h3><%= ml('Companies') %></h3>
  <ul>
   % for my $company_id(keys %{$companies} ) {
    <li>
     %= tag 'a', href => "/users/remove_company/$payload?company=$company_id", ml('Delete') ;
    </li>
