% my $id = stash('id') ;
% my $action = stash('action') ;
% my $path = "/hr/$action" ;
% $path .= "/$id" if $id ;

<div class="form">
 %= form_for $path => (method => 'POST') => begin
 %= hidden_field oname => 'hr descriptor';
 %= hidden_field id    => $id if $id ;
 <table class='colortable'>
  <tr>
   <td class='tar'> 
    %= ml 'Description' 
   </td>
   <td>
    % if( $action =~ /add|edit/i ) {
        %= text_area description => (stash 'description'), class => (stash 'description_class') 
    % } else {
        <strong><%= stash 'description' %></strong>
    % }
   </td>
  </tr>
  <tr><td>&nbsp;</td>
   <td>
     % if( $action eq 'del' ){
      %= submit_button ml('Delete') ;
      | <%= link_to "/hr/edit/$id" => begin %><%= ml 'Cancel' %><% end %>
     % }  else {
      %= submit_button ml('Save') ;
      | <%= link_to "/hr/list" => begin %><%= ml 'Cancel' %><% end %>
     % } 
   </td>
  </tr>
 </table>
 %= include stash('include_bottom') if stash('include_bottom')  ;
 % end
</div>

