% layout "default";
%= include "success_error" ;
<h2><%= ml "Templates" %> | <%= ml "Files" %></h2>

%= include "$controller/descriptor_toolbar" if who_is_global('editor') ;
%= include "$controller/descriptor_form" 
%= include "$controller/files_toolbar" if who_is_global('editor') ;
%= include "files/list" ;
<hr />
%= include "$controller/list_resources", temp_hash => stash('resources_root') if stash('resources_root') ;
