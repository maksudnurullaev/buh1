% layout "default";
%= include "success_error" ;
%# at least should be exists stash('description') - file name, for current file
% if( stash('description') ){
 %= include "utils/files_header_type1" ;

 %= include 'calculations/toolbar', add_path => "/$controller/calculations_add/$payload" if is_editor ;
 % my $link_path_prefix = "/$controller/calculations_edit/$payload?id=" ;
 %= include 'calculations/list', isClientPart => 1, link_path_prefix => $link_path_prefix ;
% } 
