% layout 'default';
<h2><%= ml 'Calculations' %></h2>
%= include 'success_error' ;

%= include 'calculations/toolbar', add_link => '/calculations/add' if who_is_global('editor') ;
% my $link_path_prefix = ("/$controller/" . ( who_is_global('editor') ? 'edit/' : 'test/')) ;
%= include 'calculations/list', isClientPart => 0, link_path_prefix => $link_path_prefix ;
