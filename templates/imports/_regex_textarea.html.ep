%= text_area definitions => ( class => 'extended', id => 'w3review' ) => begin
select 
  count(*), 
  name, 
  id, 
  field 
from 
  objects 
group by 
  name, 
  id, 
  field 
HAVING 
  COUNT(*)> 1 
  and name <> '_link_';
% end
<br /><br />