%  if ( my $parts = stash('parts') ) {
  %= include 'imports/_lex_accounts_treeview', parts => $parts, lang => 'rus' ;
% } elsif ( my $operations = stash('operations')) {
  %= include 'imports/_lex_operations_view', operations => $operations, lang => 'rus' if $operations;
% } 
