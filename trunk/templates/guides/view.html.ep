% layout 'default';
<h2><%= ml('Guide') . ' ' . $payload %></h2>
%= include 'success_error' ;
<div class="toolbar">
 %= link_to ml('Back') => '/guides/page' ; 
 % if( is_admin ){
  |
  %= link_to ml('Edit') => "/guides/edit/$payload" ;
 % } 
</div>

% if( $guide_data ){
 %= include 'guides/as_table' if $guide_data ;
% } else {
 <div class="error"><%=ml 'Error' %></div>
% }
