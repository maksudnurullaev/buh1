<div>
 % my $path = stash('path') || "/$filter4/list" ;
 %= form_for "/$filter4/filter?path=$path" => ( method => 'POST') => begin
  %= text_field filter => (stash 'filter')
  %= submit_button ml 'Filter'
  %= link_to ml("Reset") => "/$filter4/nofilter?path=$path"
 % end 
</div>

