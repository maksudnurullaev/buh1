% my $files = stash 'files' ;
% if( $files ){
 <ul>
  % for my $file (keys %{$files}){
   <li><%= $files->{$file}{name} %></li>
   % if( $files->{$file}{desc} ){
    <ul><li><pre><%= $files->{$file}{desc} %></pre></li></ul>
   % }
  % }
 </pre>
% }
